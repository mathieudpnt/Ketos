stages:
- build
- test
- deploy

#before_script:
# - apt-get install python

run-tests:
 type: test
 script:
  - pytest
 only:
  - branches

create-docs:
 type: deploy
 script:
  - cd docs/ && make html && cd ..
 only:
  - branches

create-package:
 type: deploy
 script:
  - python setup.py sdist
  - pip install dist/*
 only:
  - branches